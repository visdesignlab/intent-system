import React, {FC} from 'react';

interface OwnProps {}

type Props = OwnProps;

const SingleScatterplot: FC<Props> = ({}: Props) => {
  return (
    <g>
      <text>Test</text>
    </g>
  );
};

export default SingleScatterplot;
